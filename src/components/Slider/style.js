import styled, { css } from "styled-components";

export const Container = styled.div`
    padding: 0 20px;
    overflow: hidden;
    background: radial-gradient(circle, rgba(134,131,132,1) 23%, rgba(1,13,28,1) 75%);

    h2 {
        font-size: 28px;
        color: #fff;
        margin: 50px 0 20px 20px;
    }

  .swiper-wrapper {
    display: flex;
    gap: 10px;
  }
  
`
